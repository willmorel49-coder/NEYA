feat(p1-s2): QuizScreen Quit + preload sliding-window + aria-labels

Sprint 2 du roadmap — pure code wins, pas de dépendance externe.

QuizScreen:
- Bouton x Quitter top-left + window.confirm — débloque utilisateur
  piégé dans 23 questions (#9 backlog, RICE 60)
- Sliding-window preload: charge en arrière-plan bg-quiz-{idx+1, idx+2}
  via new Image() au changement de question — supprime le flash blanc
  + LCP 1-2s par transition

Accessibilité (WCAG 4.1.2 Name, Role, Value):
- aria-label sur tous les icons interactifs sans texte:
  HomeScreen Personnaliser ton espace
  CoconScreen Fermer le cocon
  WorldDetailOverlay Fermer
  BreathingModal/RoutineGuideModal Fermer (x2)
  BreathingModal Arreter, Pause/Reprendre (dynamique selon etat)
  PersonalizationModal Annuler les changements

Performance:
- Constante module-level MOTE_LIMIT = IS_SMALL ? 8 : 25 exposee
  (a brancher dans SplashScreen et autres ecrans riches en motes)
- apple-touch-icon link ajoute dans index.html
