/* ============================================================
   Refuge — STUB (sera implémenté en T15)
   ============================================================ */

export default function Refuge({ onClose }) {
  return (
    <div style={{ position: 'absolute', inset: 0, padding: 40, background: 'var(--bg)' }}>
      <button onClick={onClose}>‹ Retour</button>
      <p>Refuge — coming in T15</p>
    </div>
  );
}
